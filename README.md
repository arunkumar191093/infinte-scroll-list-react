# Steps to run the prodject

- open terminal and run comment `node server/server.js`, keep it running since it is running the API server
- open another terminal and run `npm install` then run `npm run start`